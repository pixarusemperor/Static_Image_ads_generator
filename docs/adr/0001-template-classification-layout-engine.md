# 0001. Template Classification Layout Engine

We considered whether the Image Analyzer should dynamically estimate arbitrary pixel coordinates (dynamic JSON layout) or classify ads into predefined layout categories. We decided to use predefined template classification because it guarantees visual alignment, robust typography, and premium aesthetics for direct-response marketing variants, trading arbitrary design flexibility for rendering reliability.
