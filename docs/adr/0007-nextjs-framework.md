# 0007. Next.js Core Application Framework

We evaluated a unified Next.js TypeScript application against a split FastAPI backend and React Vite frontend. We decided to use Next.js (App Router, TypeScript, and Tailwind CSS) as the core framework. This creates a unified codebase where React components double as both browser editor layers and server-side Satori image rendering templates, simplifying routing, state sync, and deployment.
